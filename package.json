{
  "name": "notion-presentation",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fmt": "npx prettier --write .",
    "zip": "vite build && mkdir -p out && rm -f ./out/app.zip && zip -r ./out/app.zip dist"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^1.0.14",
    "prettier": "2.8.4",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "simpledotcss": "^2.1.2"
  }
}
